spring.application.name=server


## PostgreSQL Connection
spring.datasource.url=jdbc:postgresql://overload.postgres.database.azure.com:5432/overload?sslmode=require
spring.datasource.username=nicknorsekPostGres
spring.datasource.password=TestPass123!

spring.datasource.driver-class-name=org.postgresql.Driver


# Auto update tables
spring.jpa.hibernate.ddl-auto=update

# Prints SQL
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Hot reload
spring.devtools.restart.enabled=true
spring.devtools.livereload.enabled=true

# JWT
# 1 hour in milliseconds
jwt.expiration = 4600000
# Should be 32 bytes only letters and numbers
jwt.secret = kvjwoIHEH9083KJ203HVAEFhcAOE23rhoh

# Logs

# Shorter log pattern — removes the long package paths
logging.pattern.console=%clr(%d{HH:mm:ss}){faint} %clr(%-5level) %clr(%-20logger{20}){cyan} : %msg%n

# Only show WARN and above for Spring internals — cuts the noise
logging.level.root=WARN
logging.level.com.overload=DEBUG
logging.level.org.springframework.web=WARN
logging.level.org.hibernate=WARN